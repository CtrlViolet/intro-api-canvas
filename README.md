# 🎨 Dibujo de Rectángulo con Canvas en JavaScript

## 📌 Descripción del proyecto
Este proyecto utiliza la API Canvas de HTML5 junto con JavaScript para dibujar un rectángulo dentro de un lienzo gráfico en una página web.  
El programa ajusta dinámicamente el tamaño del canvas al 50% del ancho y alto de la pantalla, y aplica estilos visuales mediante CSS.

---

## 🧠 Tecnologías utilizadas
- HTML5  
- CSS3  
- JavaScript  
- API Canvas  

---

## ⚙️ Funcionamiento del programa
1. Se obtiene el elemento `<canvas>` del documento HTML mediante su identificador.
2. Se ajusta el tamaño del canvas al 50% del ancho y alto de la ventana del navegador.
3. Se obtiene el contexto de dibujo en dos dimensiones (2D).
4. Se define el color de relleno del rectángulo.
5. Se dibuja un rectángulo en el lienzo con coordenadas y dimensiones definidas.

---

## 📂 Estructura del proyecto

```text
/ProyectoCanvas
│
├── index.html
├── script.js
├── estilos.css
└── README.md
